overwrite: true
schema: 'http://localhost:8080/graphql'
generates:
   src/modules/_shared/schema.ts:
      plugins:
         - typescript
         - typescript-resolvers
      config:
         maybeValue: T | undefined | null
